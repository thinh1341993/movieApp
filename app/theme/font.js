import { distance } from './distance'

export const font = {
    s12: 12 / distance.uiWidth * distance.windowWidth,
    s17: 17 / distance.uiWidth * distance.windowWidth,
    s14: 14 / distance.uiWidth * distance.windowWidth,
    s15: 15 / distance.uiWidth * distance.windowWidth,
    s19: 19 / distance.uiWidth * distance.windowWidth,
    s13: 13 / distance.uiWidth * distance.windowWidth,
    s37: 37 / distance.uiWidth * distance.windowWidth,
    s27: 27 / distance.uiWidth * distance.windowWidth,
    s16: 16 / distance.uiWidth * distance.windowWidth,
}